\documentclass{article}

% set the text to english explicitly
\usepackage[english]{babel}

% margin settings
\usepackage[a4paper,margin=1cm]{geometry}

% allow precise placement of figures
\usepackage{float}

% background drawing settings
\usepackage{tikz}
\usepackage{eso-pic}

% the multi column support
\usepackage{paracol}

% utilities
\usepackage{lipsum} % for placeholder gibberish
\usepackage{fontawesome5} % for cool icons

% for font support
\usepackage{fontspec}

% we want to use nunito
\newfontfamily\titlefont{Nunito Light}

% size of various parts
\setlength{\columnsep}{1cm} % important for columns
\columnratio{0.3,0.7}
\setlength{\textfloatsep}{5pt}
\setlength{\intextsep}{5pt}
\setlength{\parskip}{0pt}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% drawing the actual BG
\newcommand{\drawBackground}[1]{
  \AddToShipoutPictureBG{%
    \begin{tikzpicture}[remember picture,overlay]
      \fill[#1!20]
      (current page.south west)
      rectangle ([xshift=0.33\paperwidth]current page.north west);
    \end{tikzpicture}%
  }
}

% we want a way to draw a proper title
\makeatletter
\renewcommand{\maketitle}{
  \noindent
  {\titlefont\Huge\@author} \\
  \medskip \\
}
\makeatother

\drawBackground{blue}

\author{Your Name}
\date{today}

\begin{document}
\begin{paracol}{2}
  \switchcolumn[0]
  \sloppy
  \lipsum[1]
  \switchcolumn
  \maketitle
  \lipsum{1}
\end{paracol}

\end{document}